function out = testQ1(a, b, c, d, n)
    
    try
        mdl = fitgmdist(a, n);
    catch exception
        mdl = fitgmdist(a, n, 'SharedCov', true);
        disp('shared');
    end
    
%     p = pdf(mdl, c);
    [p,nlogL] = posterior(mdl, c) 
    disp(p);
    disp(p);
    out = 0.5;
end